require 'rails_helper'

RSpec.describe "Zoidbergs", type: :request do
  describe "GET /planet_express_zoidbergs" do
    it "works! (now write some real specs)" do
      skip "fix routing"
      get planet_express_zoidbergs_path
      expect(response.status).to be(200)
    end
  end
end
